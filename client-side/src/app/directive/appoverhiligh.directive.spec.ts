import { AppoverhilighDirective } from './appoverhiligh.directive';

describe('AppoverhilighDirective', () => {
  it('should create an instance', () => {
    const directive = new  AppoverhilighDirective();
    expect(directive).toBeTruthy();
  });
});
